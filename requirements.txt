# Core RAG dependencies
langchain>=0.3
langchain-community>=0.3
langchain-huggingface>=0.1
langchain-qdrant>=0.2
langchain-google-genai>=2.0
langchain-openai>=0.2
langchain-text-splitters>=0.3
langchain-experimental>=0.3

# Vector store
qdrant-client>=1.11

# Embeddings & LLM backends
sentence-transformers>=3.0
transformers>=4.45
# CPU-only torch: Streamlit Cloud has no GPU; CPU wheel keeps torch.cuda.is_available()=False
# so accelerate skips pynvml and avoids "Found no NVIDIA driver" on upload.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu

# Template rendering
jinja2>=3.1

# API server
fastapi>=0.115
uvicorn[standard]>=0.32

# CLI
typer>=0.13

# UI
streamlit>=1.40
httpx>=0.27

# Settings
pydantic>=2.9
pydantic-settings>=2.6

# PDF loading
pypdf>=5.0

# Evaluation dependencies are in requirements-eval.txt
# Install separately: pip install -r requirements-eval.txt
